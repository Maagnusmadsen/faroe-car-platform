# Messaging

Messaging between owners and renters (Step E12).

**When to add here:**
- Thread/conversation creation (e.g. per booking).
- Send message, list messages, mark read.
- Optional: real-time (WebSocket or polling).

API routes will call message service; this folder holds service/helpers.
